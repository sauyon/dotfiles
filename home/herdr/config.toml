# Declared here because herdr persists this by rewriting config.toml in place,
# which fails on the read-only home-manager symlink.
onboarding = false

[keys]
prefix = "ctrl+z"

[ui]
# Agent panel ordering: "spaces" (grouped by space) or "priority" (attention queue).
agent_panel_sort = "priority"
